[project]
name = "thesis-build"
version = "0.1.0"
description = "Local build tooling for the Antares AI thesis"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
build = "build:main"

[dependency-groups]
dev = []
